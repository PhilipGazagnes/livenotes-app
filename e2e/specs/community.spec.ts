import { test, expect } from '../fixtures'

// Placeholder tests — fill in as you build community features.
// test.fixme marks them as "not yet implemented" so they show in the report.
test.describe('Community', () => {
  test.fixme('user A can see user B public library', async () => {})
  test.fixme('user B can join a project', async () => {})
  test.fixme('user A and user B see the same setlist after a change', async () => {})
})
