# Netlify SPA redirect rules
# This ensures that all routes are handled by the Vue Router (client-side routing)
# Redirects all requests to index.html, except for files that actually exist

/*    /index.html   200
